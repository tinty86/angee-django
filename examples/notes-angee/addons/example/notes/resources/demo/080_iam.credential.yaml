# A placeholder credential for the local-template VCS integration (082). The local
# backend reads the filesystem and authenticates nothing, but an Integration requires a
# credential FK — so this is an unused static token. Owned by the demo admin (010).
- xref: cred_local_templates
  user: notes.user_admin
  kind: static_token
  name: local-templates
  material: '{"api_key": "unused"}'
