# Angee addon manifest. The declarative contract hatch-angee compiles into
# the package's [project] + entry points; see angee/addons.py (AddonContract).
[addon]
name = "angee.knowledge_graph_pgvector"
description = "Source app manifest for the knowledge semantic-retrieval plugin."
keywords = ["pgvector", "embeddings", "knowledge"]
category = "Integration"
depends_on = ["angee.knowledge", "angee.mcp"]
