{
  "name": "angee",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.1.3",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "codegen": "pnpm --filter @angee-example/notes-host codegen",
    "pretypecheck": "pnpm codegen",
    "typecheck": "pnpm -r typecheck",
    "pretest": "pnpm codegen",
    "test": "pnpm -r test",
    "prebuild": "pnpm codegen",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "@graphql-typed-document-node/core": "^3.2.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
